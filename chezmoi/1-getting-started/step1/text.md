# Step 1

## Install

```bash
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply --interactive lwbt/killercoda-chezmoi-dotfiles
```{{exec}}

## Verify

Please create file `/tmp/step1`:

```
touch /tmp/step1
```{{exec}}
